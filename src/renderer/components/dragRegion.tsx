import * as React from 'react';



/**
 * Drag region hidden at top of page
 */
export class DragRegion extends React.Component {
  public render(): React.ReactNode {
    return (
      <div className='drag-region' />
    );
  }
}
